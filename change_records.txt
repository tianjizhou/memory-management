10/14/2017 11PM
Code changes:
1. ready_queue.h and ready_queue.cpp: added function--void PrintPIDs();
2. ready_queue.cpp: modifie push(std::vector<Process*> new_processes, const std::string& mode, int t);
3. clock.h: added function--void PrintTime();
4. cpu.h: added function--int get_half_cs()
5. cpu.h & cpu.cpp:  changed function pop() to pop( Clock clk , ReadyQueue ready_queue ) ;
6. main.cpp: print out events

Simulation results:
Used p1-input01.txt as input file
    (1) FCFS and SRT: right. ( Last line is different. Clock keeps ticking for a while even after all processes terminate)
    (2) RR: wrong.

To-do-lists:
1. add output for preemptions
2. debug RR algorithm
3. statistics for each algorithm


